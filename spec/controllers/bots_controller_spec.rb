describe BotsController do

  describe 'GET #index' do

    it "returns all bots for that user" do
      # get :index, id: 'product', format: 'pdf'
      # expect(response.headers['Content-Type']).to have_content 'application/pdf'
    end

  end

end
